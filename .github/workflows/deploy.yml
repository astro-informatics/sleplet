---
name: Deploy

# yamllint disable-line rule:truthy
on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      # yamllint disable-line rule:line-length
      - uses: paddyroddy/.github/actions/python/deployment@931cbf5aea2aceae87072826c7053fa2c6491911 # v0
        with:
          pyproject-toml: ./pyproject.toml
          python-version: "3.11"
