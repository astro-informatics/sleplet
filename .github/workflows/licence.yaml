---
name: Update Copyright

on:
  schedule:
    - cron: 0 3 1 1 *

jobs:
  update-licence-year:
    permissions:
      contents: read
    runs-on: ubuntu-slim
    steps:
      # yamllint disable-line rule:line-length
      - uses: paddyroddy/.github/actions/licence@3b5a73012982bdeb04d02bcf81cc28bdfe5d4ee5 # v0
        with:
          licence-file: ./LICENCE.txt
          # yamllint disable-line rule:line-length
          licence-token: ${{ secrets.LICENCE_PAT }} # zizmor: ignore[secrets-outside-env]
